<div id="form">
  <%= simple_form_for @model,url: model_path(@model.id), validate: true do |f| %>
  <%=render "form", f: f%>
  <%=f.button :submit, "Зберегти", class:"btn btn-success" %>
  <%end%>

</div>
